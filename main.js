const changeButtonText = (text, buttonId) => {
    //if (text === String && buttonId === String) {
        const buttonChangeBg = document.getElementById(buttonId);
        buttonChangeBg.textContent = text;
    //};
}
